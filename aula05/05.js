// Exemplo 1: Repetição simples com WHILE
var contador = 1;
while (contador <= 3) {
    alert("Executando a repetição número: " + contador);
    contador++; // Atualização para evitar loop infinito
}
